# PawPadIO Hub

## Setup Development Environment:

1. Download .NET Core SDK
2. Clone Repo
3. Run `dotnet restore` to get all third-party dependencies
4. Run `dotnet build src/PawPadIO.Hub/API` to compile
4. Run `dotnet run src/PawPadIO.Hub/API` to run the project